import React from 'react';
import { createRoot } from 'react-dom/client';
import { QuoteEditorPage } from './pages/QuoteEditorPage';

createRoot(document.getElementById('root')!).render(<QuoteEditorPage />);
